export const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  REGISTER: '/register',
  UNAUTHORIZED: '/unauthorized',
  ADMIN: {
    DOCUMENTS: '/admin/documents',
    SETTINGS: '/admin/settings',
  },
} as const;